import IOutWritter from './IOutWritter';
import IConfiguration from './IConfiguration';

/**
 * CvsResultsWritter
 */

export default class CvsResultsWritter implements IOutWritter {
    
    Initialize(configuration: IConfiguration){
        
    }
    
    Write(message:string){
        
    }
    
    Finish(){
        
    }
}